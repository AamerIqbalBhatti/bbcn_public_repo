# archive/

Python files retired from the active codebase. They are **not** on any live pipeline
path (dead duplicates, superseded prototypes, and exploratory scripts) and are kept here
only for provenance. Nothing in the reproduction pipeline (`run_all.py`) imports them.
